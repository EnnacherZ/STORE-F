# Legacy storefront-core experiment

This directory is a stale extraction experiment and is not part of the Vite
runtime. The production application is entirely under `src/`, and `tsconfig`
includes only that directory.

Do not implement fixes here: they will not reach customers. The directory is
kept temporarily because removing the tracked copy requires explicit repository
cleanup approval. It can be deleted once that approval is given.
